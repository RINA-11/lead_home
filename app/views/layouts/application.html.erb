<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <title>Lead Home</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome-animation/0.0.10/font-awesome-animation.css" type="text/css" media="all" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.7.1/css/lightbox.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/3ddeb9fb8d.js" crossorigin="anonymous"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.7.1/js/lightbox.min.js" type="text/javascript"></script>
  </head>

  <body>
    <nav class="navbar navbar-default" id="top">
      <div class="container">
        <div class="navbar-header">
          <%= link_to "Lead Home", home_top_path, class: "navbar-brand" %>
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar" aria-controls="navbar" aria-expanded="false">
            <span class="sr-only">メニュー</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div id="navbar" class="navbar-right collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li>
              <%= link_to posts_path, class: "nav-link" do %>
                <div class="fas fa-list-alt">掲示板へ</div>
              <% end %>
            </li>
            <% if logged_in? %>
              <li>
                <%= link_to new_post_path, class: "nav-link" do %>
                  <div class="fas fa-edit">投稿する</div>
                <% end %>
              </li>
              <li>
                <%= link_to clips_path, class: "nav-link" do %>
                  <div class="fas fa-paperclip">クリップ一覧</div>
                <% end %>
              </li>
              <li>
                <%= link_to user_path(current_user.id), class: "nav-link" do %>
                  <div class="fas fa-user">マイページ</div>
                <% end %>
              </li>
              <li>
                <%= link_to session_path(current_user.id), method: :delete, class: "nav-link" do %>
                  <div class="fas fa-sign-out-alt">ログアウト</div>
                <% end %>
              </li>
            <% else %>
              <li>
                <%= link_to  new_user_path, class: "nav-link" do %>
                  <div class="fas fa-user-plus">ユーザー登録</div>
                <% end %>
              </li>
              <li>
                <%= link_to new_session_path, class: "nav-link" do %>
                  <div class="fas fa-sign-in-alt">ログイン</div>
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %> alert-dismissible" role="alert">
        <%= value %>
        <button type="button" class="close" data-dismiss="alert" aria-label="閉じる">
          <span aria-hidden="true">
            <i class="fas fa-times"></i>
          </span>
        </button>
      </div>
    <% end %>
    <div class="container wrapper">
      <%= yield %>
    </div>
    <a class="far fa-caret-square-up" href="#top"></a>
    <script type="text/javascript">LineIt.loadButton();</script>
  </body>
</html>