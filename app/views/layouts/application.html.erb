<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <title>Lead Home</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome-animation/0.0.10/font-awesome-animation.css" type="text/css" media="all" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.7.1/css/lightbox.css" rel="stylesheet">
    <script src="https://kit.fontawesome.com/3ddeb9fb8d.js" crossorigin="anonymous"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lightbox2/2.7.1/js/lightbox.min.js" type="text/javascript"></script>
    <script type="text/javascript">LineIt.loadButton();</script>
  </head>

  <body>
    <nav class="navbar navbar-default">
      <div class="container">
        <div class="navbar-header">
          <%= link_to "Lead Home", home_top_path, class: "navbar-brand" %>
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar" aria-controls="navbar" aria-expanded="false">
            <span class="sr-only">メニュー</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div id="navbar" class="navbar-right collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li>
              <%= link_to " 掲示板へ", posts_path, class: "nav-link fas fa-list-alt" %>
            </li>
            <% if logged_in? %>
              <li>
                <%= link_to "投稿する", new_post_path, class: "nav-link fas fa-edit" %>
              </li>
              <li>
                <%= link_to "クリップ一覧", clips_path, class: "nav-link fas fa-paperclip" %>
              </li>
              <li>
                <%= link_to "マイページ", user_path(current_user.id), class: "nav-link fas fa-user" %>
              </li>
              <li>
                <%= link_to "ログアウト", session_path(current_user.id), method: :delete, class: "nav-link fas fa-sign-out-alt" %>
              </li>
            <% else %>
              <li>
                <%= link_to "ユーザー登録", new_user_path, class: "nav-link fas fa-user-plus" %>
              </li>
              <li>
                <%= link_to "ログイン", new_session_path, class: "nav-link fas fa-sign-in-alt" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %>" role="alert"><%= value %></div>
    <% end %>
    <div class="container wrapper">
      <%= yield %>
    </div>
  </body>
</html>